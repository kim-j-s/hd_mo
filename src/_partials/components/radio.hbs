<div class="inp_radio">
	<input type="radio"
		id="{{id}}"
		name="{{name}}"
		{{#if checked}}checked{{/if}}
		{{#if disabled}}disabled{{/if}}
	>
	<label for="{{id}}"><span>{{{labelname}}}</span></label>
</div>