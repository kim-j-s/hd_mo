<div class="acd_item" {{#if each}}{{/if}}>
  {{#if each}}
  <div class="acd_head{{#if active}} active{{/if}}" {{#if active}}aria-expanded="true"{{else}}aria-expanded="false"{{/if}}>
    <button type="button"><span>{{{ac-title}}}</span></button>
  </div>
  <div class="acd_cont">
    <div class="inner"{{#if active}} style="display:inline-block;"{{/if}}>
      {{> @partial-block }}
    </div>
  </div>
  {{/if}}
  <div class="acd_head{{#if active}} active{{/if}}" {{#if active}}aria-expanded="true"{{else}}aria-expanded="false"{{/if}}>
    <button type="button"><span>{{{ac-title}}}</span></button>
  </div>
  <div class="acd_cont">
    <div class="inner"{{#if active}} style="display:inline-block;"{{/if}}>
      {{> @partial-block }}
    </div>
  </div>
</div>