<div id="header" class="header">
	<div class="header_inner{{#unless h-back}} history_back_none{{/unless}}">

		{{#if main}}
		<h1 class="header_logo">
			<a href="#"><img src="" alt="현대해상 다이렉트"></a>
		</h1>
		{{/if}}

		{{#unless no-header}}

		<div class="header_left">
		{{#unless h-back}}
			<button type="button" class="history_back"><span class="text">이전 페이지로 이동</span></button>
		{{/unless}}
		{{#if h-home}}
			<button type="button" class="home"><span class="text">홈으로 이동</span></button>
		{{/if}}
		</div>

		{{#if h-title}}
		<div class="header_connect">
			<h1 class="header_title">{{h-title}}</h1>
		</div>
		{{/if}}

		<div class="header_right">
			{{!-- 계약변경 팝업 호출 버튼 --}}
			{{#if h-ins}}
				<button type="button" class="insChk" onclick="openHDPopup(this,'pop_insChk');"><span class="text">계약변경 팝업 열기</span></button>
			{{/if}}
			{{!-- 계약변경 팝업 호출 버튼 --}}
			{{#unless no-nav}}
			<button type="button" class="allmenu_open"><span class="text">전체메뉴 열기</span></button>
			{{/unless}}
			{{!-- s: 전체메뉴 --}}
			{{#unless no-nav}}
			{{> layout/nav }}
			{{/unless}}
			{{!-- e: 전체메뉴 --}}
		</div>

		{{/unless}}


	</div>
			
</div>